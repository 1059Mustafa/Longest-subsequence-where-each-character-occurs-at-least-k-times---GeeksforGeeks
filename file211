void findSubsequence(string str, int k)
{
	int cnt[MAX_CHAR]={0};
  for(int i=0;i<str.size();i++)
  {
    cnt[str[i]-'a']++;
  }
  for(int i=0;i<26;i++)
  {
    if(cnt[str[i]-'a']>=k)
    {
      cout<<str[i];
    }
  }
  return;
      
      
      
      
}
